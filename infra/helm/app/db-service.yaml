apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: default
  labels:
    app: database
spec:
  selector:
    app: database
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  clusterIP: None    # Headless service: obrigatório para StatefulSets com DNS estável
