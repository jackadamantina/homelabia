apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: default
  labels:
    app: api-pod
spec:
  selector:
    app: api-pod
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
